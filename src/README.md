# Source

This directory is reserved for future importable project code.

## Planned package

```text
msm_cognition/
  vision/       Screenshot and icon-matching logic
  rules/        Breeding rules and reference-model helpers
  inference/    Result-ranking and decision-support logic
```

Current repo-maintenance scripts live in `scripts/`.
